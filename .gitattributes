# บังคับให้ไฟล์ Shell Script ใช้ LF เสมอ (ป้องกันรันบน Linux/Docker ไม่ได้)
*.sh text eol=lf

# ตั้งค่าพื้นฐานให้ Git จัดการการขึ้นบรรทัดให้อัตโนมัติสำหรับไฟล์ข้อความอื่นๆ
* text=auto

# ระบุประเภทไฟล์เฉพาะทาง (เผื่อคุณมีไฟล์พวกนี้ในอนาคต)
*.yaml text
*.yml text
*.json text
*.py text