apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "project-app.fullname" . }}-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: {{ .Values.pvc.size }}